Name: to call the private repo resuable file

on: 
  push:
    branches: [ main ]
  
jobs:
  call-reuse-workflow:
    uses: $GITHUB_WORKSPACE/hubactions/.github/workflows/demo.yml@v3.0
    with:
      tarfilename: vijay
